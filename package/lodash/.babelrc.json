{
  "presets": [
    [
      "@babel/env", 
      { 
        "modules": false,
        "targets": {
          "chrome": "58", // 默认60以上, 不会将 ()=> 转换为 function() {}
          "ie": "11"
        }
      }
    ]
  ]
}